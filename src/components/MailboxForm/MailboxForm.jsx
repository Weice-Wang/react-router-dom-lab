import { useState } from "react";

const initialState = {
  name: "",
  weight: 0,
  height: 0,
};

const MailboxForm = (props) => {
  return (
    <>
      <label htmlFor="boxSize">Boxholder:</label>
      <input type="boxOwner" name="boxOwner" value />
    </>
  );
};

export default MailboxForm;
